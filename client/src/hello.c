#include <stdio.h>
#include <math.h>  // Needed for fmod()

int main() {
    int  x = 1;
    print(x);
    x += 1;
    printf(x)
    x += 1;
    printf(x)
    x += 1;
    printf(x)
    x += 1;
    printf(x)
    x += 1;
    printf(x)
    x += 1;
    printf(x)

    return 0;
}
